<Window Icon="{StaticResource ChordiousIcon}"
        MinWidth="440"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Title="{Binding Title, FallbackValue=STYLE EDITOR}"
        WindowStartupLocation="CenterScreen"
        x:Class="Chordious.WPF.DiagramStyleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:local="clr-namespace:Chordious.WPF"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit">
    <Window.InputBindings>
        <KeyBinding Command="{Binding Cancel, Mode=OneWay}"
                    Key="Escape" />
    </Window.InputBindings>
    <StackPanel Background="Transparent"
                Focusable="True"
                PreviewMouseDown="FocusOn_PreviewMouseDown">
        <StackPanel>
            <StackPanel Orientation="Horizontal"
                        Width="500">
                <Label Content="{Binding SelectedStyleLabel, FallbackValue=SELECTED STYLE}"
                       FontWeight="Bold"
                       Margin="5"
                       Name="StyleLabel"
                       Target="{Binding ElementName=StyleComboBox}"
                       ToolTip="{Binding SelectedStyleToolTip}"
                       VerticalAlignment="Center"
                       Width="220" />
                <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=StyleLabel}"
                          ItemsSource="{Binding Styles}"
                          Margin="5"
                          Name="StyleComboBox"
                          SelectedIndex="{Binding SelectedStyleIndex}"
                          Style="{StaticResource CleanComboBox}"
                          ToolTip="{Binding SelectedStyleToolTip}"
                          VerticalAlignment="Center"
                          Width="220">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Level}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
            <GroupBox Header="{Binding Style.SummaryLabel, FallbackValue=SUMMARY}"
                      Margin="5"
                      ToolTip="{Binding Style.SummaryToolTip}"
                      Width="480">
                <TextBox AutomationProperties.Name="{Binding Style.SummaryLabel}"
                         Height="60"
                         HorizontalScrollBarVisibility="Disabled"
                         IsManipulationEnabled="False"
                         IsReadOnly="True"
                         Margin="5"
                         Text="{Binding Style.Summary, Mode=OneWay}"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" />
            </GroupBox>
        </StackPanel>
        <TabControl Margin="5">
            <TabItem Header="{Binding Style.DiagramGroupLabel, FallbackValue=DIAGRAM}">
                <StackPanel IsEnabled="{Binding Style.IsEditable}"
                            Orientation="Horizontal">
                    <StackPanel>
                        <GroupBox Header="{Binding Style.DiagramLayoutGroupLabel, FallbackValue=DIAGRAM LAYOUT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedOrientationLabel, FallbackValue=ORIENTATION}"
                                              Height="25"
                                              IsChecked="{Binding Style.OrientationIsLocal}"
                                              Margin="5"
                                              Name="OrientationIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedOrientationToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=OrientationIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.OrientationIsLocal}"
                                              ItemsSource="{Binding Style.Orientations}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedOrientationIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedOrientationToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedLabelLayoutModelLabel, FallbackValue=LABEL MODEL}"
                                              Height="25"
                                              IsChecked="{Binding Style.LabelLayoutModelIsLocal}"
                                              Margin="5"
                                              Name="LabelLayoutModelIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedLabelLayoutModelToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=LabelLayoutModelIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.LabelLayoutModelIsLocal}"
                                              ItemsSource="{Binding Style.LabelLayoutModels}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedLabelLayoutModelIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedLabelLayoutModelToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="{Binding Style.DiagramBackgroundGroupLabel, FallbackValue=DIAGRAM BACKGROUND}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.DiagramColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.DiagramColorIsLocal}"
                                              Margin="5"
                                              Name="DiagramColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.DiagramColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=DiagramColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.DiagramColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.DiagramColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.DiagramColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.DiagramColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.DiagramOpacityLabel, FallbackValue=OPACITY}"
                                              Height="25"
                                              IsChecked="{Binding Style.DiagramOpacityIsLocal}"
                                              Margin="5"
                                              Name="DiagramOpacityIsLocalCheckBox"
                                              ToolTip="{Binding Style.DiagramOpacityToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=DiagramOpacityIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.DiagramOpacityIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.DiagramOpacityToolTip}"
                                                       Value="{Binding Style.DiagramOpacity}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                    <StackPanel>
                        <GroupBox Header="{Binding Style.NewDiagramGroupLabel, FallbackValue=NEW DIAGRAMS}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.NewDiagramNumStringsLabel, FallbackValue=# OF STRINGS}"
                                              Height="25"
                                              IsChecked="{Binding Style.NewDiagramNumStringsIsLocal}"
                                              Margin="5"
                                              Name="NewDiagramNumStringsIsLocalCheckBox"
                                              ToolTip="{Binding Style.NewDiagramNumStringsToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:IntegerUpDown AutomationProperties.LabeledBy="{Binding ElementName=NewDiagramNumStringsIsLocalCheckBox}"
                                                        Height="25"
                                                        HorizontalContentAlignment="Center"
                                                        IsEnabled="{Binding Style.NewDiagramNumStringsIsLocal}"
                                                        Margin="5"
                                                        Minimum="2"
                                                        ToolTip="{Binding Style.NewDiagramNumStringsToolTip}"
                                                        Value="{Binding Style.NewDiagramNumStrings}"
                                                        VerticalAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.NewDiagramNumFretsLabel, FallbackValue=# OF FRETS}"
                                              Height="25"
                                              IsChecked="{Binding Style.NewDiagramNumFretsIsLocal}"
                                              Margin="5"
                                              Name="NewDiagramNumFretsIsLocalCheckBox"
                                              ToolTip="{Binding Style.NewDiagramNumFretsToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:IntegerUpDown AutomationProperties.LabeledBy="{Binding ElementName=NewDiagramNumFretsIsLocalCheckBox}"
                                                        Height="25"
                                                        HorizontalContentAlignment="Center"
                                                        IsEnabled="{Binding Style.NewDiagramNumFretsIsLocal}"
                                                        Margin="5"
                                                        Minimum="1"
                                                        ToolTip="{Binding Style.NewDiagramNumFretsToolTip}"
                                                        Value="{Binding Style.NewDiagramNumFrets}"
                                                        VerticalAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="{Binding Style.DiagramBorderGroupLabel, FallbackValue=DIAGRAM BORDER}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.DiagramBorderColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.DiagramBorderColorIsLocal}"
                                              Margin="5"
                                              Name="DiagramBorderColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.DiagramBorderColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=DiagramBorderColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.DiagramBorderColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.DiagramBorderColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.DiagramBorderColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.DiagramBorderColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.DiagramBorderThicknessLabel, FallbackValue=THICKNESS (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.DiagramBorderThicknessIsLocal}"
                                              Margin="5"
                                              Name="DiagramBorderThicknessIsLocalCheckBox"
                                              ToolTip="{Binding Style.DiagramBorderThicknessToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=DiagramBorderThicknessIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.DiagramBorderThicknessIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.DiagramBorderThicknessToolTip}"
                                                       Value="{Binding Style.DiagramBorderThickness}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="{Binding Style.GridGroupLabel, FallbackValue=GRID}">
                <StackPanel IsEnabled="{Binding Style.IsEditable}"
                            Orientation="Horizontal">
                    <StackPanel>
                        <GroupBox Header="{Binding Style.GridSpacingGroupLabel, FallbackValue=GRID SPACINGS}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridFretSpacingLabel, FallbackValue=FRET SPACING (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridFretSpacingIsLocal}"
                                              Margin="5"
                                              Name="GridFretSpacingIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridFretSpacingToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridFretSpacingIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridFretSpacingIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridFretSpacingToolTip}"
                                                       Value="{Binding Style.GridFretSpacing}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridStringSpacingLabel, FallbackValue=STRING SPACING (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridStringSpacingIsLocal}"
                                              Margin="5"
                                              Name="GridStringSpacingIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridStringSpacingToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridStringSpacingIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridStringSpacingIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridStringSpacingToolTip}"
                                                       Value="{Binding Style.GridStringSpacing}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="{Binding Style.GridMarginGroupLabel, FallbackValue=GRID MARGINS}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridMarginLabel, FallbackValue=BASE (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridMarginIsLocal}"
                                              Margin="5"
                                              Name="GridMarginIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridMarginToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridMarginIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridMarginIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridMarginToolTip}"
                                                       Value="{Binding Style.GridMargin}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridMarginLeftLabel, FallbackValue=LEFT (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridMarginLeftIsLocal}"
                                              Margin="5"
                                              Name="GridMarginLeftIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridMarginLeftToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridMarginLeftIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridMarginLeftIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridMarginLeftToolTip}"
                                                       Value="{Binding Style.GridMarginLeft}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridMarginRightLabel, FallbackValue=RIGHT (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridMarginRightIsLocal}"
                                              Margin="5"
                                              Name="GridMarginRightIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridMarginRightToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridMarginRightIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridMarginRightIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridMarginRightToolTip}"
                                                       Value="{Binding Style.GridMarginRight}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridMarginTopLabel, FallbackValue=TOP (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridMarginTopIsLocal}"
                                              Margin="5"
                                              Name="GridMarginTopIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridMarginTopToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridMarginTopIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridMarginTopIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridMarginTopToolTip}"
                                                       Value="{Binding Style.GridMarginTop}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridMarginBottomLabel, FallbackValue=BOTTOM (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridMarginBottomIsLocal}"
                                              Margin="5"
                                              Name="GridMarginBottomIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridMarginBottomToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridMarginBottomIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridMarginBottomIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridMarginBottomToolTip}"
                                                       Value="{Binding Style.GridMarginBottom}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                    <StackPanel>
                        <GroupBox Header="{Binding Style.GridBackgroundGroupLabel, FallbackValue=GRID BACKGROUND}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.GridColorLabel, FallbackValue=COLOR}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.GridColorIsLocal}"
                                                  Margin="5"
                                                  Name="GridColorIsLocalCheckBox"
                                                  ToolTip="{Binding Style.GridColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=GridColorIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEditable="True"
                                                  IsEnabled="{Binding Style.GridColorIsLocal}"
                                                  ItemsSource="{Binding Style.Colors}"
                                                  Margin="5"
                                                  SelectedItem="{Binding Style.GridColor}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  Text="{Binding Style.GridColor, UpdateSourceTrigger=LostFocus}"
                                                  ToolTip="{Binding Style.GridColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.GridOpacityLabel, FallbackValue=OPACITY}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.GridOpacityIsLocal}"
                                                  Margin="5"
                                                  Name="GridOpacityIsLocalCheckBox"
                                                  ToolTip="{Binding Style.GridOpacityToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridOpacityIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.GridOpacityIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource BoundPercentageUpDown}"
                                                           ToolTip="{Binding Style.GridOpacityToolTip}"
                                                           Value="{Binding Style.GridOpacity}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="{Binding Style.GridLineGroupLabel, FallbackValue=GRID LINES}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridLineColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridLineColorIsLocal}"
                                              Margin="5"
                                              Name="GridLineColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridLineColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=GridLineColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.GridLineColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.GridLineColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.GridLineColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.GridLineColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridLineThicknessLabel, FallbackValue=THICKNESS (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridLineThicknessIsLocal}"
                                              Margin="5"
                                              Name="GridLineThicknessIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridLineThicknessToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridLineThicknessIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridLineThicknessIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.GridLineThicknessToolTip}"
                                                       Value="{Binding Style.GridLineThickness}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Focusable="True"
                                  Header="{Binding Style.GridNutGroupLabel, FallbackValue=GRID NUT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridNutVisibleLabel, FallbackValue=VISIBLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridNutVisibleIsLocal}"
                                              Margin="5"
                                              Name="GridNutVisibleIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridNutVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=GridNutVisibleIsLocalCheckBox}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridNutVisible}"
                                              IsEnabled="{Binding Style.GridNutVisibleIsLocal}"
                                              Margin="5"
                                              ToolTip="{Binding Style.GridNutVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.GridNutRatioLabel, FallbackValue=RATIO}"
                                              Height="25"
                                              IsChecked="{Binding Style.GridNutRatioIsLocal}"
                                              Margin="5"
                                              Name="GridNutRatioIsLocalCheckBox"
                                              ToolTip="{Binding Style.GridNutRatioToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=GridNutRatioIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.GridNutRatioIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PositivePercentageUpDown}"
                                                       ToolTip="{Binding Style.GridNutRatioToolTip}"
                                                       Value="{Binding Style.GridNutRatio}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="{Binding Style.TitleGroupLabel, FallbackValue=TITLE}">
                <StackPanel IsEnabled="{Binding Style.IsEditable}"
                            Orientation="Horizontal">
                    <StackPanel>
                        <GroupBox Header="{Binding Style.TitleTextGroupLabel, FallbackValue=TITLE TEXT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleFontFamilyLabel, FallbackValue=FONT}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleFontFamilyIsLocal}"
                                              Margin="5"
                                              Name="TitleFontFamilyIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleFontFamilyToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=TitleFontFamilyIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.TitleFontFamilyIsLocal}"
                                              ItemsSource="{Binding Style.FontFamilies}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.TitleFontFamily}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.TitleFontFamily, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.TitleFontFamilyToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedTitleTextStyleLabel, FallbackValue=STYLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleTextStyleIsLocal}"
                                              Margin="5"
                                              Name="TitleTextStyleIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedTitleTextStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=TitleTextStyleIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.TitleTextStyleIsLocal}"
                                              ItemsSource="{Binding Style.TitleTextStyles}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedTitleTextStyleIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedTitleTextStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleTextSizeLabel, FallbackValue=SIZE (PT)}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleTextSizeIsLocal}"
                                              Margin="5"
                                              Name="TitleTextSizeIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleTextSizeToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=TitleTextSizeIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.TitleTextSizeIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.TitleTextSizeToolTip}"
                                                       Value="{Binding Style.TitleTextSize}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleColorIsLocal}"
                                              Margin="5"
                                              Name="TitleColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=TitleColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.TitleColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.TitleColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.TitleColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.TitleColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleOpacityLabel, FallbackValue=OPACITY}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleOpacityIsLocal}"
                                              Margin="5"
                                              Name="TitleOpacityIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleOpacityToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=TitleOpacityIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.TitleOpacityIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.TitleOpacityToolTip}"
                                                       Value="{Binding Style.TitleOpacity}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedTitleLabelStyleLabel, FallbackValue=LABEL STYLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleLabelStyleIsLocal}"
                                              Margin="5"
                                              Name="TitleLabelStyleIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedTitleLabelStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=TitleLabelStyleIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.TitleLabelStyleIsLocal}"
                                              ItemsSource="{Binding Style.TitleLabelStyles}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedTitleLabelStyleIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedTitleLabelStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleTextSizeModRatioLabel, FallbackValue=MODIFIER RATIO}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleTextSizeModRatioIsLocal}"
                                              Margin="5"
                                              Name="TitleTextSizeModRatioIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleTextSizeModRatioToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=TitleTextSizeModRatioIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.TitleTextSizeModRatioIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.TitleTextSizeModRatioToolTip}"
                                                       Value="{Binding Style.TitleTextSizeModRatio}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                    <StackPanel>
                        <GroupBox Header="{Binding Style.TitleLayoutGroupLabel, FallbackValue=TITLE LAYOUT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleVisibleLabel, FallbackValue=VISIBLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleVisibleIsLocal}"
                                              Margin="5"
                                              Name="TitleVisibleIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=TitleVisibleIsLocalCheckBox}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleVisible}"
                                              IsEnabled="{Binding Style.TitleVisibleIsLocal}"
                                              Margin="5"
                                              ToolTip="{Binding Style.TitleVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedTitleTextAlignmentLabel, FallbackValue=ALIGNMENT}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleTextAlignmentIsLocal}"
                                              Margin="5"
                                              Name="TitleTextAlignmentIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedTitleTextAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=TitleTextAlignmentIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.TitleTextAlignmentIsLocal}"
                                              ItemsSource="{Binding Style.TitleTextAlignments}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedTitleTextAlignmentIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedTitleTextAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.TitleGridPaddingLabel, FallbackValue=GRID PADDING (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.TitleGridPaddingIsLocal}"
                                              Margin="5"
                                              Name="TitleGridPaddingIsLocalCheckBox"
                                              ToolTip="{Binding Style.TitleGridPaddingToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=TitleGridPaddingIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.TitleGridPaddingIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.TitleGridPaddingToolTip}"
                                                       Value="{Binding Style.TitleGridPadding}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="{Binding Style.MarksGroupLabel, FallbackValue=MARKS}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="{Binding Style.SelectedMarkTypeLabel, FallbackValue=SELECTED MARK TYPE}"
                               FontWeight="Bold"
                               Height="25"
                               Margin="5"
                               Name="MarkTypeLabel"
                               Target="{Binding ElementName=MarkTypeComboBox}"
                               ToolTip="{Binding Style.SelectedMarkTypeToolTip}"
                               VerticalAlignment="Center"
                               VerticalContentAlignment="Center"
                               Width="130" />
                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkTypeLabel}"
                                  Height="25"
                                  ItemsSource="{Binding Style.MarkTypes}"
                                  Margin="5"
                                  Name="MarkTypeComboBox"
                                  SelectedIndex="{Binding Style.SelectedMarkTypeIndex}"
                                  Style="{StaticResource CleanComboBox}"
                                  ToolTip="{Binding Style.SelectedMarkTypeToolTip}"
                                  VerticalAlignment="Center"
                                  VerticalContentAlignment="Center"
                                  Width="82" />
                    </StackPanel>
                    <StackPanel IsEnabled="{Binding Style.IsEditable}"
                                Orientation="Horizontal">
                        <StackPanel>
                            <GroupBox Header="{Binding Style.MarkBackgroundGroupLabel, FallbackValue=MARK BACKGROUND}"
                                      Margin="5">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkVisibleLabel, FallbackValue=VISIBLE}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkVisibleIsLocal}"
                                                  Margin="5"
                                                  Name="MarkVisibleIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkVisibleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=MarkVisibleIsLocalCheckBox}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkVisible}"
                                                  IsEnabled="{Binding Style.MarkVisibleIsLocal}"
                                                  Margin="5"
                                                  ToolTip="{Binding Style.MarkVisibleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.SelectedMarkShapeLabel, FallbackValue=SHAPE}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkShapeIsLocal}"
                                                  Margin="5"
                                                  Name="MarkShapeIsLocalCheckBox"
                                                  ToolTip="{Binding Style.SelectedMarkShapeToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkShapeIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEnabled="{Binding Style.MarkShapeIsLocal}"
                                                  ItemsSource="{Binding Style.MarkShapes}"
                                                  Margin="5"
                                                  SelectedIndex="{Binding Style.SelectedMarkShapeIndex}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  ToolTip="{Binding Style.SelectedMarkShapeToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkColorLabel, FallbackValue=COLOR}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkColorIsLocal}"
                                                  Margin="5"
                                                  Name="MarkColorIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkColorIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEditable="True"
                                                  IsEnabled="{Binding Style.MarkColorIsLocal}"
                                                  ItemsSource="{Binding Style.Colors}"
                                                  Margin="5"
                                                  SelectedItem="{Binding Style.MarkColor}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  Text="{Binding Style.MarkColor, UpdateSourceTrigger=LostFocus}"
                                                  ToolTip="{Binding Style.MarkColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkOpacityLabel, FallbackValue=OPACITY}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkOpacityIsLocal}"
                                                  Margin="5"
                                                  Name="MarkOpacityIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkOpacityToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=MarkOpacityIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.MarkOpacityIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource BoundPercentageUpDown}"
                                                           ToolTip="{Binding Style.MarkOpacityToolTip}"
                                                           Value="{Binding Style.MarkOpacity}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkRadiusRatioLabel, FallbackValue=RADIUS RATIO}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkRadiusRatioIsLocal}"
                                                  Margin="5"
                                                  Name="MarkRadiusRatioIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkRadiusRatioToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=MarkRadiusRatioIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.MarkRadiusRatioIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource BoundPercentageUpDown}"
                                                           ToolTip="{Binding Style.MarkRadiusRatioToolTip}"
                                                           Value="{Binding Style.MarkRadiusRatio}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="{Binding Style.MarkBorderGroupLabel, FallbackValue=MARK BORDER}"
                                      Margin="5">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkBorderColorLabel, FallbackValue=COLOR}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkBorderColorIsLocal}"
                                                  Margin="5"
                                                  Name="MarkBorderColorIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkBorderColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkBorderColorIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEditable="True"
                                                  IsEnabled="{Binding Style.MarkBorderColorIsLocal}"
                                                  ItemsSource="{Binding Style.Colors}"
                                                  Margin="5"
                                                  SelectedItem="{Binding Style.MarkBorderColor}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  Text="{Binding Style.MarkBorderColor, UpdateSourceTrigger=LostFocus}"
                                                  ToolTip="{Binding Style.MarkBorderColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkBorderThicknessLabel, FallbackValue=THICKNESS (PX)}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkBorderThicknessIsLocal}"
                                                  Margin="5"
                                                  Name="MarkBorderThicknessIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkBorderThicknessToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=MarkBorderThicknessIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.MarkBorderThicknessIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource PixelUpDown}"
                                                           ToolTip="{Binding Style.MarkBorderThicknessToolTip}"
                                                           Value="{Binding Style.MarkBorderThickness}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                        <StackPanel>
                            <GroupBox Header="{Binding Style.MarkTextGroupLabel, FallbackValue=MARK TEXT}"
                                      Margin="5">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkTextVisibleLabel, FallbackValue=VISIBLE}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextVisibleIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextVisibleIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkTextVisibleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=MarkTextVisibleIsLocalCheckBox}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextVisible}"
                                                  IsEnabled="{Binding Style.MarkTextVisibleIsLocal}"
                                                  Margin="5"
                                                  ToolTip="{Binding Style.MarkTextVisibleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.SelectedMarkTextAlignmentLabel, FallbackValue=ALIGNMENT}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextAlignmentIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextAlignmentIsLocalCheckBox"
                                                  ToolTip="{Binding Style.SelectedMarkTextAlignmentToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkTextAlignmentIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEnabled="{Binding Style.MarkTextAlignmentIsLocal}"
                                                  ItemsSource="{Binding Style.MarkTextAlignments}"
                                                  Margin="5"
                                                  SelectedIndex="{Binding Style.SelectedMarkTextAlignmentIndex}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  ToolTip="{Binding Style.SelectedMarkTextAlignmentToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkFontFamilyLabel, FallbackValue=FONT}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkFontFamilyIsLocal}"
                                                  Margin="5"
                                                  Name="MarkFontFamilyIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkFontFamilyToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkFontFamilyIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEditable="True"
                                                  IsEnabled="{Binding Style.MarkFontFamilyIsLocal}"
                                                  ItemsSource="{Binding Style.FontFamilies}"
                                                  Margin="5"
                                                  SelectedItem="{Binding Style.MarkFontFamily}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  Text="{Binding Style.MarkFontFamily, UpdateSourceTrigger=LostFocus}"
                                                  ToolTip="{Binding Style.MarkFontFamilyToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.SelectedMarkTextStyleLabel, FallbackValue=STYLE}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextStyleIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextStyleIsLocalCheckBox"
                                                  ToolTip="{Binding Style.SelectedMarkTextStyleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkTextStyleIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEnabled="{Binding Style.MarkTextStyleIsLocal}"
                                                  ItemsSource="{Binding Style.MarkTextStyles}"
                                                  Margin="5"
                                                  SelectedIndex="{Binding Style.SelectedMarkTextStyleIndex}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  ToolTip="{Binding Style.SelectedMarkTextStyleToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkTextColorLabel, FallbackValue=COLOR}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextColorIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextColorIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkTextColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=MarkTextColorIsLocalCheckBox}"
                                                  Height="25"
                                                  IsEditable="True"
                                                  IsEnabled="{Binding Style.MarkTextColorIsLocal}"
                                                  ItemsSource="{Binding Style.Colors}"
                                                  Margin="5"
                                                  SelectedItem="{Binding Style.MarkTextColor}"
                                                  Style="{StaticResource CleanComboBox}"
                                                  Text="{Binding Style.MarkTextColor, UpdateSourceTrigger=LostFocus}"
                                                  ToolTip="{Binding Style.MarkTextColorToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkTextOpacityLabel, FallbackValue=OPACITY}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextOpacityIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextOpacityIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkTextOpacityToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=MarkTextOpacityIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.MarkTextOpacityIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource BoundPercentageUpDown}"
                                                           ToolTip="{Binding Style.MarkTextOpacityToolTip}"
                                                           Value="{Binding Style.MarkTextOpacity}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox Content="{Binding Style.MarkTextSizeRatioLabel, FallbackValue=SIZE RATIO}"
                                                  Height="25"
                                                  IsChecked="{Binding Style.MarkTextSizeRatioIsLocal}"
                                                  Margin="5"
                                                  Name="MarkTextSizeRatioIsLocalCheckBox"
                                                  ToolTip="{Binding Style.MarkTextSizeRatioToolTip}"
                                                  VerticalAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  Width="120" />
                                        <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=MarkTextSizeRatioIsLocalCheckBox}"
                                                           Height="25"
                                                           HorizontalContentAlignment="Center"
                                                           IsEnabled="{Binding Style.MarkTextSizeRatioIsLocal}"
                                                           Margin="5"
                                                           Style="{StaticResource BoundPercentageUpDown}"
                                                           ToolTip="{Binding Style.MarkTextSizeRatioToolTip}"
                                                           Value="{Binding Style.MarkTextSizeRatio}"
                                                           VerticalAlignment="Center"
                                                           VerticalContentAlignment="Center"
                                                           Width="80" />
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="{Binding Style.FretLabelsGroupLabel, FallbackValue=FRET LABELS}">
                <StackPanel IsEnabled="{Binding Style.IsEditable}"
                            Orientation="Horizontal">
                    <StackPanel>
                        <GroupBox Header="{Binding Style.FretLabelTextGroupLabel, FallbackValue=FRET LABEL TEXT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelFontFamilyLabel, FallbackValue=FONT}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelFontFamilyIsLocal}"
                                              Margin="5"
                                              Name="FretLabelFontFamilyIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelFontFamilyToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=FretLabelFontFamilyIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.FretLabelFontFamilyIsLocal}"
                                              ItemsSource="{Binding Style.FontFamilies}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.FretLabelFontFamily}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.FretLabelFontFamily, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.FretLabelFontFamilyToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedFretLabelTextStyleLabel, FallbackValue=STYLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextStyleIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextStyleIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedFretLabelTextStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextStyleIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.FretLabelTextStyleIsLocal}"
                                              ItemsSource="{Binding Style.FretLabelTextStyles}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedFretLabelTextStyleIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedFretLabelTextStyleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelTextColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextColorIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelTextColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.FretLabelTextColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.FretLabelTextColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.FretLabelTextColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.FretLabelTextColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelTextOpacityLabel, FallbackValue=OPACITY}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextOpacityIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextOpacityIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelTextOpacityToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextOpacityIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.FretLabelTextOpacityIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.FretLabelTextOpacityToolTip}"
                                                       Value="{Binding Style.FretLabelTextOpacity}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelTextSizeRatioLabel, FallbackValue=SIZE RATIO}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextSizeRatioIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextSizeRatioIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelTextSizeRatioToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextSizeRatioIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.FretLabelTextSizeRatioIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.FretLabelTextSizeRatioToolTip}"
                                                       Value="{Binding Style.FretLabelTextSizeRatio}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelTextWidthRatioLabel, FallbackValue=WIDTH RATIO}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextWidthRatioIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextWidthRatioIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelTextWidthRatioToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextWidthRatioIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.FretLabelTextWidthRatioIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.FretLabelTextWidthRatioToolTip}"
                                                       Value="{Binding Style.FretLabelTextWidthRatio}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                    <StackPanel>
                        <GroupBox Header="{Binding Style.FretLabelLayoutGroupLabel, FallbackValue=FRET LABEL LAYOUT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelTextVisibleLabel, FallbackValue=VISIBLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextVisibleIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextVisibleIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelTextVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextVisibleIsLocalCheckBox}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextVisible}"
                                              IsEnabled="{Binding Style.FretLabelTextVisibleIsLocal}"
                                              Margin="5"
                                              ToolTip="{Binding Style.FretLabelTextVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedFretLabelTextAlignmentLabel, FallbackValue=ALIGNMENT}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelTextAlignmentIsLocal}"
                                              Margin="5"
                                              Name="FretLabelTextAlignmentIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedFretLabelTextAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=FretLabelTextAlignmentIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.FretLabelTextAlignmentIsLocal}"
                                              ItemsSource="{Binding Style.FretLabelTextAlignments}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedFretLabelTextAlignmentIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedFretLabelTextAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.FretLabelGridPaddingLabel, FallbackValue=GRID PADDING (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.FretLabelGridPaddingIsLocal}"
                                              Margin="5"
                                              Name="FretLabelGridPaddingIsLocalCheckBox"
                                              ToolTip="{Binding Style.FretLabelGridPaddingToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=FretLabelGridPaddingIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.FretLabelGridPaddingIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.FretLabelGridPaddingToolTip}"
                                                       Value="{Binding Style.FretLabelGridPadding}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="{Binding Style.BarresGroupLabel, FallbackValue=BARRES}">
                <StackPanel IsEnabled="{Binding Style.IsEditable}"
                            Orientation="Horizontal">
                    <StackPanel>
                        <GroupBox Header="{Binding Style.BarreStyleGroupLabel, FallbackValue=BARRE STYLE}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.BarreArcRatioLabel, FallbackValue=ARC RATIO}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreArcRatioIsLocal}"
                                              Margin="5"
                                              Name="BarreArcRatioIsLocalCheckBox"
                                              ToolTip="{Binding Style.BarreArcRatioToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=BarreArcRatioIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.BarreArcRatioIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PositivePercentageUpDown}"
                                                       ToolTip="{Binding Style.BarreArcRatioToolTip}"
                                                       Value="{Binding Style.BarreArcRatio}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.BarreOpacityLabel, FallbackValue=OPACITY}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreOpacityIsLocal}"
                                              Margin="5"
                                              Name="BarreOpacityIsLocalCheckBox"
                                              ToolTip="{Binding Style.BarreOpacityToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=BarreOpacityIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.BarreOpacityIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource BoundPercentageUpDown}"
                                                       ToolTip="{Binding Style.BarreOpacityToolTip}"
                                                       Value="{Binding Style.BarreOpacity}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.BarreLineColorLabel, FallbackValue=COLOR}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreLineColorIsLocal}"
                                              Margin="5"
                                              Name="BarreLineColorIsLocalCheckBox"
                                              ToolTip="{Binding Style.BarreLineColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=BarreLineColorIsLocalCheckBox}"
                                              Height="25"
                                              IsEditable="True"
                                              IsEnabled="{Binding Style.BarreLineColorIsLocal}"
                                              ItemsSource="{Binding Style.Colors}"
                                              Margin="5"
                                              SelectedItem="{Binding Style.BarreLineColor}"
                                              Style="{StaticResource CleanComboBox}"
                                              Text="{Binding Style.BarreLineColor, UpdateSourceTrigger=LostFocus}"
                                              ToolTip="{Binding Style.BarreLineColorToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.BarreLineThicknessLabel, FallbackValue=THICKNESS (PX)}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreLineThicknessIsLocal}"
                                              Margin="5"
                                              Name="BarreLineThicknessIsLocalCheckBox"
                                              ToolTip="{Binding Style.BarreLineThicknessToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <xctk:DoubleUpDown AutomationProperties.LabeledBy="{Binding ElementName=BarreLineThicknessIsLocalCheckBox}"
                                                       Height="25"
                                                       HorizontalContentAlignment="Center"
                                                       IsEnabled="{Binding Style.BarreLineThicknessIsLocal}"
                                                       Margin="5"
                                                       Style="{StaticResource PixelUpDown}"
                                                       ToolTip="{Binding Style.BarreLineThicknessToolTip}"
                                                       Value="{Binding Style.BarreLineThickness}"
                                                       VerticalAlignment="Center"
                                                       VerticalContentAlignment="Center"
                                                       Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                    <StackPanel>
                        <GroupBox Header="{Binding Style.BarreLayoutGroupLabel, FallbackValue=BARRE LAYOUT}"
                                  Margin="5">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.BarreVisibleLabel, FallbackValue=VISIBLE}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreVisibleIsLocal}"
                                              Margin="5"
                                              Name="BarreVisibleIsLocalCheckBox"
                                              ToolTip="{Binding Style.BarreVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <CheckBox AutomationProperties.LabeledBy="{Binding ElementName=BarreVisibleIsLocalCheckBox}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreVisible}"
                                              IsEnabled="{Binding Style.BarreVisibleIsLocal}"
                                              Margin="5"
                                              ToolTip="{Binding Style.BarreVisibleToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedBarreVerticalAlignmentLabel, FallbackValue=ALIGNMENT}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreVerticalAlignmentIsLocal}"
                                              Margin="5"
                                              Name="BarreVerticalAlignmentIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedBarreVerticalAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=BarreVerticalAlignmentIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.BarreVerticalAlignmentIsLocal}"
                                              ItemsSource="{Binding Style.BarreVerticalAlignments}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedBarreVerticalAlignmentIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedBarreVerticalAlignmentToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Style.SelectedBarreStackLabel, FallbackValue=STACK}"
                                              Height="25"
                                              IsChecked="{Binding Style.BarreStackIsLocal}"
                                              Margin="5"
                                              Name="BarreStackIsLocalCheckBox"
                                              ToolTip="{Binding Style.SelectedBarreStackToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="120" />
                                    <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=BarreStackIsLocalCheckBox}"
                                              Height="25"
                                              IsEnabled="{Binding Style.BarreStackIsLocal}"
                                              ItemsSource="{Binding Style.BarreStacks}"
                                              Margin="5"
                                              SelectedIndex="{Binding Style.SelectedBarreStackIndex}"
                                              Style="{StaticResource CleanComboBox}"
                                              ToolTip="{Binding Style.SelectedBarreStackToolTip}"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              Width="80" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
        <WrapPanel FlowDirection="RightToLeft"
                   Height="Auto">
            <StackPanel FlowDirection="LeftToRight"
                        Orientation="Horizontal">
                <Button AutomationProperties.Name="{Binding Style.ResetLabel}"
                        Command="{Binding Style.Reset}"
                        IsEnabled="{Binding Style.IsEditable}"
                        Margin="5"
                        Style="{StaticResource CleanButtonNoBorder}"
                        ToolTip="{Binding Style.ResetToolTip}">
                    <local:AutoDisableImage Height="16"
                                            Source="{StaticResource ResetIcon16}"
                                            Width="16" />
                </Button>
                <Button Command="{Binding Apply}"
                        Content="{Binding AppVM.ApplyLabel, FallbackValue=APPLY}"
                        Margin="5"
                        Style="{StaticResource CleanButton}"
                        Width="75" />
                <Button Command="{Binding Accept}"
                        Content="{Binding AppVM.AcceptLabel, FallbackValue=OK}"
                        Margin="5"
                        Style="{StaticResource CleanButton}"
                        Width="75" />
                <Button Command="{Binding Cancel}"
                        Content="{Binding AppVM.CancelLabel, FallbackValue=CANCEL}"
                        Margin="5"
                        Style="{StaticResource CleanButton}"
                        Width="75" />
            </StackPanel>
        </WrapPanel>
    </StackPanel>
</Window>
